# PostgreSQL host-based authentication — Docker Compose mTLS
# Mounted via hba_file; local socket used by pg_isready healthchecks.

# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     trust
hostssl all             all             all                     scram-sha-256 clientcert=verify-full
